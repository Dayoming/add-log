<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.add.log.dao.LogDao">
    <!--  배치 로그 등록  -->
    <insert id="insertBatchLogs">
        INSERT INTO public.log_batches (
            batch_id,
            client_id,
            session_id,
            game_version,
            schema_version,
            sent_at
        ) VALUES (
            #{batchId},
            #{clientId},
            #{sessionId},
            #{gameVersion},
            #{schemaVersion},
            #{sentAt}
        )
    </insert>

    <!--  게임 로그 등록  -->
    <insert id="insertGameLogs">
        INSERT INTO public.game_logs (
            log_id,
            batch_id,
            run_id,
            event_name,
            event_version,
            occurred_at,
            sequence,
            session_elapsed_ms,
            run_elapsed_ms,
            stage_elapsed_ms,
            stage_id,
            playthrough_count,
            payload
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.logId},
                #{item.batchId},
                #{item.runId, jdbcType=VARCHAR},
                #{item.eventName},
                #{item.eventVersion},
                #{item.occurredAt},
                #{item.sequence},
                #{item.sessionElapsedMs},
                #{item.runElapsedMs, jdbcType=INTEGER},
                #{item.stageElapsedMs, jdbcType=INTEGER},
                #{item.stageId, jdbcType=VARCHAR},
                #{item.playthroughCount},
                #{item.payload, typeHandler=com.add.log.util.JsonTypeHandler}
            )
        </foreach>
    </insert>
</mapper>
