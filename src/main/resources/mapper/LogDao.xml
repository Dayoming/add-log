<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.add.log.dao.LogDao">
    <!--  배치 로그 등록  -->
    <insert id="insertBatchLogs">
        INSERT INTO public.batch_logs (
            batch_id,
            client_id,
            session_id,
            game_version,
            schema_version,
            sent_at
        ) VALUES (
            #{batchId},
            #{clientId},
            #{sessionId},
            #{gameVersion},
            #{schemaVersion},
            #{sentAt}
        )
    </insert>

    <!--  게임 로그 등록  -->
    <insert id="insertGameLogs">
        INSERT INTO public.common_game_logs (
            log_id,
            batch_id,
            run_id,
            event_name,
            event_version,
            occurred_at,
            sequence,
            session_elapsed_ms,
            run_elapsed_ms,
            stage_elapsed_ms,
            stage_id,
            stage_clear_count,
            playthrough_count,
            payload
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.logId},
                #{item.batchId},
                #{item.runId, jdbcType=VARCHAR},
                #{item.eventName},
                #{item.eventVersion},
                #{item.occurredAt},
                #{item.sequence},
                #{item.sessionElapsedMs},
                #{item.runElapsedMs, jdbcType=INTEGER},
                #{item.stageElapsedMs, jdbcType=INTEGER},
                #{item.stageId, jdbcType=VARCHAR},
                #{item.stageClearCount, jdbdType=INTEGER},
                #{item.playthroughCount},
                #{item.payload, typeHandler=com.add.log.handler.JsonTypeHandler}
            )
        </foreach>
        ON CONFLICT (log_id) DO NOTHING
    </insert>

    <!--  세션 로그 등록  -->
    <insert id="insertSessionLogs" parameterType="list">
        INSERT INTO public.session_logs (
            log_id,
            batch_id,
            session_id,
            event_name,
            occurred_at,
            platform,
            operating_system,
            system_language,
            session_count,
            is_first_session,
            end_reason,
            total_session_time_ms
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.logId, jdbcType=VARCHAR},
                #{item.batchId, jdbcType=VARCHAR},
                #{item.sessionId, jdbcType=VARCHAR},
                #{item.eventName, jdbcType=VARCHAR},
                #{item.occurredAt, jdbcType=TIMESTAMP},
                #{item.platform, jdbcType=VARCHAR},
                #{item.operatingSystem, jdbcType=VARCHAR},
                #{item.systemLanguage, jdbcType=VARCHAR},
                #{item.sessionCount, jdbcType=INTEGER},
                #{item.isFirstSession, jdbcType=BOOLEAN},
                #{item.endReason, jdbcType=VARCHAR},
                #{item.totalSessionTimeMs, jdbcType=BIGINT}
            )
        </foreach>
    </insert>

    <!--  런 로그 등록  -->
    <insert id="insertRunLogs" parameterType="list">
        INSERT INTO run_logs (
            log_id,
            run_id,
            batch_id,
            session_id,
            event_name,
            occurred_at,
            playthrough_count,
            start_type,
            starting_stage_id,
            continue_run,
            end_reason,
            ending_id,
            game_over_type,
            final_stage_id,
            total_run_time_ms,
            final_work_discipline,
            final_audit_risk
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.logId, jdbcType=VARCHAR},
                #{item.runId, jdbcType=VARCHAR},
                #{item.batchId, jdbcType=VARCHAR},
                #{item.sessionId, jdbcType=VARCHAR},
                #{item.eventName, jdbcType=VARCHAR},
                #{item.occurredAt, jdbcType=TIMESTAMP},
                #{item.playthroughCount, jdbcType=INTEGER},
                #{item.startType, jdbcType=VARCHAR},
                #{item.startingStageId, jdbcType=VARCHAR},
                #{item.continueRun, jdbcType=BOOLEAN},
                #{item.endReason, jdbcType=VARCHAR},
                #{item.endingId, jdbcType=VARCHAR},
                #{item.gameOverType, jdbcType=VARCHAR},
                #{item.finalStageId, jdbcType=VARCHAR},
                #{item.totalRunTimeMs, jdbcType=BIGINT},
                #{item.finalWorkDiscipline, jdbcType=INTEGER},
                #{item.finalAuditRisk, jdbcType=INTEGER}
            )
        </foreach>
    </insert>

    <!--  스테이지 로그 등록  -->
    <insert id="insertStageLogs" parameterType="list">
        INSERT INTO stage_logs (
            log_id,
            run_id,
            stage_id,
            batch_id,
            session_id,
            event_name,
            occurred_at,
            stage_attempt,
            loaded_game,
            end_reason,
            total_stage_time_ms,
            next_stage_id,
            final_work_discipline,
            final_audit_risk
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.logId, jdbcType=VARCHAR},
                #{item.runId, jdbcType=VARCHAR},
                #{item.stageId, jdbcType=VARCHAR},
                #{item.batchId, jdbcType=VARCHAR},
                #{item.sessionId, jdbcType=VARCHAR},
                #{item.eventName, jdbcType=VARCHAR},
                #{item.occurredAt, jdbcType=TIMESTAMP},
                #{item.stageAttempt, jdbcType=INTEGER},
                #{item.loadedGame, jdbcType=BOOLEAN},
                #{item.endReason, jdbcType=VARCHAR},
                #{item.totalStageTimeMs, jdbcType=BIGINT},
                #{item.nextStageId, jdbcType=VARCHAR},
                #{item.finalWorkDiscipline, jdbcType=INTEGER},
                #{item.finalAuditRisk, jdbcType=INTEGER}
            )
        </foreach>
    </insert>

    <!--  스탯 로그 등록  -->
    <insert id="insertStatLogs" parameterType="list">
        INSERT INTO stat_logs (
            log_id,
            run_id,
            stage_id,
            batch_id,
            session_id,
            event_name,
            occurred_at,
            stat_type,
            before_value,
            requested_delta,
            applied_delta,
            after_value,
            source_type,
            source_id,
            source_action_id,
            reason_id,
            caused_game_over
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.logId, jdbcType=VARCHAR},
                #{item.runId, jdbcType=VARCHAR},
                #{item.stageId, jdbcType=VARCHAR},
                #{item.batchId, jdbcType=VARCHAR},
                #{item.sessionId, jdbcType=VARCHAR},
                #{item.eventName, jdbcType=VARCHAR},
                #{item.occurredAt, jdbcType=TIMESTAMP},
                #{item.statType, jdbcType=VARCHAR},
                #{item.beforeValue, jdbcType=INTEGER},
                #{item.requestedDelta, jdbcType=INTEGER},
                #{item.appliedDelta, jdbcType=INTEGER},
                #{item.afterValue, jdbcType=INTEGER},
                #{item.sourceType, jdbcType=VARCHAR},
                #{item.sourceId, jdbcType=VARCHAR},
                #{item.sourceActionId, jdbcType=VARCHAR},
                #{item.reasonId, jdbcType=VARCHAR},
                #{item.causedGameOver, jdbcType=BOOLEAN}
            )
        </foreach>
    </insert>

    <!--  이벤트 로그 등록  -->
    <insert id="insertEventQuestLogs" parameterType="list">
        INSERT INTO event_quest_logs (
            log_id,
            run_id,
            stage_id,
            batch_id,
            session_id,
            event_name,
            occurred_at,
            content_type,
            event_id,
            choice_id,
            has_quest,
            result_type
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.logId, jdbcType=VARCHAR},
                #{item.runId, jdbcType=VARCHAR},
                #{item.stageId, jdbcType=VARCHAR},
                #{item.batchId, jdbcType=VARCHAR},
                #{item.sessionId, jdbcType=VARCHAR},
                #{item.eventName, jdbcType=VARCHAR},
                #{item.occurredAt, jdbcType=TIMESTAMP},
                #{item.contentType, jdbcType=VARCHAR},
                #{item.eventId, jdbcType=VARCHAR},
                #{item.choiceId, jdbcType=VARCHAR},
                #{item.hasQuest, jdbcType=BOOLEAN},
                #{item.resultType, jdbcType=VARCHAR}
            )
        </foreach>
    </insert>

    <!--  튜토리얼 로그 등록  -->
    <insert id="insertTutorialLogs" parameterType="list">
        INSERT INTO tutorial_logs (
            log_id,
            run_id,
            stage_id,
            batch_id,
            session_id,
            client_id,
            event_name,
            occurred_at,
            tutorial_id,
            step_id,
            step_order,
            step_time_ms,
            total_tutorial_time_ms,
            completed_step_count,
            skipped_at_step_id,
            skipped_at_step_order,
            elapsed_before_skip_ms,
            playthrough_count
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.logId, jdbcType=VARCHAR},
                #{item.runId, jdbcType=VARCHAR},
                #{item.stageId, jdbcType=VARCHAR},
                #{item.batchId, jdbcType=VARCHAR},
                #{item.sessionId, jdbcType=VARCHAR},
                #{item.clientId, jdbcType=VARCHAR},
                #{item.eventName, jdbcType=VARCHAR},
                #{item.occurredAt, jdbcType=TIMESTAMP},
                #{item.tutorialId, jdbcType=VARCHAR},
                #{item.stepId, jdbcType=VARCHAR},
                #{item.stepOrder, jdbcType=INTEGER},
                #{item.stepTimeMs, jdbcType=BIGINT},
                #{item.totalTutorialTimeMs, jdbcType=BIGINT},
                #{item.completedStepCount, jdbcType=INTEGER},
                #{item.skippedAtStepId, jdbcType=VARCHAR},
                #{item.skippedAtStepOrder, jdbcType=INTEGER},
                #{item.elapsedBeforeSkipMs, jdbcType=BIGINT},
                #{item.playthroughCount, jdbcType=INTEGER}
            )
        </foreach>
        ON CONFLICT (client_id) WHERE event_name = 'TUTORIAL_COMPLETED' AND playthrough_count = 0
        DO NOTHING
    </insert>

    <!--  프로토콜 제작 및 사용 로그 등록  -->
    <insert id="insertEdgeBlockLogs" parameterType="list">
        INSERT INTO edge_block_logs (
            log_id,
            run_id,
            stage_id,
            batch_id,
            session_id,
            event_name,
            occurred_at,
            edge_block_id,
            usage_count
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.logId, jdbcType=VARCHAR},
                #{item.runId, jdbcType=VARCHAR},
                #{item.stageId, jdbcType=VARCHAR},
                #{item.batchId, jdbcType=VARCHAR},
                #{item.sessionId, jdbcType=VARCHAR},
                #{item.eventName, jdbcType=VARCHAR},
                #{item.occurredAt, jdbcType=TIMESTAMP},
                #{item.edgeBlockId, jdbcType=VARCHAR},
                #{item.usageCount, jdbcType=INTEGER}
            )
        </foreach>
    </insert>

    <!--  청사진 이용 로그 등록  -->
    <insert id="insertBlueprintLogs" parameterType="list">
        INSERT INTO blueprint_logs (
            log_id,
            run_id,
            stage_id,
            batch_id,
            session_id,
            event_name,
            occurred_at,
            total_open_count,
            total_load_count,
            blueprint_id,
            load_count
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.logId, jdbcType=VARCHAR},
                #{item.runId, jdbcType=VARCHAR},
                #{item.stageId, jdbcType=VARCHAR},
                #{item.batchId, jdbcType=VARCHAR},
                #{item.sessionId, jdbcType=VARCHAR},
                #{item.eventName, jdbcType=VARCHAR},
                #{item.occurredAt, jdbcType=TIMESTAMP},
                #{item.totalOpenCount, jdbcType=INTEGER},
                #{item.totalLoadCount, jdbcType=INTEGER},
                #{item.blueprintId, jdbcType=VARCHAR},
                #{item.loadCount, jdbcType=INTEGER}
            )
        </foreach>
    </insert>

    <!--  logId 조회  -->
    <select id="findExistingLogIds" resultType="string">
        SELECT log_id
        FROM common_game_logs
        WHERE log_id IN
        <foreach collection="list" item="logId" open="(" separator="," close=")">
            #{logId, jdbcType=VARCHAR}
        </foreach>
    </select>
</mapper>
